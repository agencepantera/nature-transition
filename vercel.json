{
  "buildCommand": "npx astro build && npx pagefind --site dist",
  "installCommand": "npm install",
  "framework": "astro",
  "outputDirectory": "dist",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ]
}
